pipeline {
    agent {label 'slave_one'}

    stages {
        stage('Stage 1') {

            // lable specific build
            steps {
                echo 'Hello from Stage 1!'

                sh '''
                #!/bin/bash
                ls 
                sleep 5
                echo 'This is Linux command for prod'

                '''
            }
        }

        stage('Stage 2') {
            steps {
                echo 'Hello from Stage 2! for prod'
            }
        }
    }
}